version: '2'
services:
  tkhip-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/tkhip/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=tkhip
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
